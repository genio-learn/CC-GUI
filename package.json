{
  "name": "cc-gui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "app:build": "tauri build",
    "app:install": "tauri build && rm -rf /Applications/CC-GUI.app && cp -R src-tauri/target/release/bundle/macos/CC-GUI.app /Applications/ && echo 'Installed to /Applications/CC-GUI.app'",
    "cc:pull": "git -C ../claude-commander pull --ff-only",
    "cc:update": "npm run cc:pull && npm run app:install"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0",
    "shiki": "^4.2.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "~5.6.2",
    "vite": "^6.0.3"
  }
}
